require('dotenv').config(); const { REST, Routes } = require('discord.js'); const rest = new REST().setToken(process.env.DISCORD_BOT_TOKEN); (async ()=>{ try{ await rest.put(Routes.applicationCommands(process.env.CLIENT_ID), {body:[{name:'quiz',description:'Math quiz!'},{name:'leaderboard',description:'Top scorers!'}]}); console.log('✅ Commands OK!'); }catch(e){console.log('Deploy error:',e.message);} })();
