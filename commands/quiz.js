const { SlashCommandBuilder, ActionRowBuilder, ButtonBuilder, ButtonStyle, EmbedBuilder } = require('discord.js'); const questions = [{question: 'Capital ng PH?', options:['Manila','QC','Cebu','Davao'], answer:0}, {question: 'Unang pres ng PH?', options:['Roxas','Aguinaldo','Magsaysay','Laurel'], answer:0}]; module.exports = {data: new SlashCommandBuilder().setName('quiz').setDescription('Quiz game!'), async execute(interaction) { const q = questions[Math.floor(Math.random()*questions.length)]; const row = new ActionRowBuilder().addComponents(q.options.map((opt,i)=>new ButtonBuilder().setCustomId('ans_'+i).setLabel(opt).setStyle(ButtonStyle.Primary))); const embed = new EmbedBuilder().setTitle('🧠 Quiz!').setDescription(q.question).setColor('Green'); await interaction.reply({embeds:[embed], components:[row]}); }};
